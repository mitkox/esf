[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "esf-intake"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["dspy[typesafe]==3.4.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["esf_intake*"]
