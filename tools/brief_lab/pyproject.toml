[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "esf-brief-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["dspy==3.4.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["esf_brief_lab*"]
